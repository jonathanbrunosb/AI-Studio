import Link from "next/link";
import { Layers3, ShieldCheck } from "lucide-react";

export function AuthShell({ title, description, children }: { title: string; description: string; children: React.ReactNode }) {
  return <main className="grid min-h-screen bg-slate-50 lg:grid-cols-[minmax(420px,.85fr)_minmax(520px,1.15fr)]"><section className="relative hidden overflow-hidden bg-[#081e38] p-12 text-white lg:flex lg:flex-col"><div className="absolute -right-24 -top-24 size-96 rounded-full border-[58px] border-blue-400/10" /><div className="relative flex items-center gap-3"><span className="grid size-12 place-items-center rounded-xl bg-gradient-to-br from-blue-400 to-blue-600 shadow-lg"><Layers3 size={24} /></span><div><p className="text-xl font-bold">AI Studio</p><p className="text-sm text-blue-200/70">Comunicação Contábil</p></div></div><div className="relative my-auto max-w-xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-sky-300">Ambiente corporativo</p><h1 className="mt-4 text-4xl font-bold leading-tight">Comunicação contábil com segurança, consistência e governança.</h1><p className="mt-5 text-lg leading-8 text-blue-100/70">Ambiente integrado para criação, gestão e aprovação de conteúdos da Contabilidade.</p></div><div className="relative flex items-center gap-2 text-xs text-blue-200/60"><ShieldCheck size={16} />Acesso restrito a colaboradores autorizados</div></section><section className="flex items-center justify-center p-5 sm:p-10"><div className="w-full max-w-md"><Link href="/" className="mb-10 flex items-center gap-3 lg:hidden"><span className="grid size-11 place-items-center rounded-xl bg-blue-700 text-white"><Layers3 size={22} /></span><span><strong className="block text-lg text-slate-900">AI Studio</strong><span className="text-xs text-slate-500">Comunicação Contábil</span></span></Link><div className="surface-card p-6 sm:p-8"><p className="eyebrow">Acesso seguro</p><h2 className="mt-2 text-3xl font-bold tracking-tight text-slate-900">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{description}</p><div className="mt-7">{children}</div></div><p className="mt-5 text-center text-xs text-slate-400">Gerência de Contabilidade · Ambiente corporativo restrito</p></div></section></main>;
}
